
function giu
  git status
end
function gid
  git diff
end

function gids
  git diff --stat
end
function gdw
  git diff --word-diff
end
function gdp
  git diff --patience
end
function gis
  git show
end
function giss
  git show --stat
end
function gil
  git log --graph --oneline --abbrev-commit --decorate
end
function glb
  git log --graph --oneline --abbrev-commit --decorate --branches
end
function gilb
  git log --graph --oneline --abbrev-commit --decorate --branches
end

